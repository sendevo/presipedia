import {Container} from '@mui/material';
import MainView from "../../templates/MainView";

const View = () => {
    return(
        <MainView>
            <Container>
                
            </Container>  
        </MainView>
    );
};

export default View;