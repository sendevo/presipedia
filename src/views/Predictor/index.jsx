import MainView from "../../components/MainView";

const View = () => {
    return(
        <MainView title="¿Quién será el siguiente?">
            
        </MainView>
    );
};

export default View;